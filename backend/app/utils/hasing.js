const bcrypt  = require('bcryptjs');

module.exports.encrypt = (field) => {
    return false;
};

module.exports.decrypt = (field) => {
    return false;
};

module.exports.compare = async (fieldA, fieldB) => {
    // bcrypt.compare(fieldA, fieldB, (err, result) => {
    //
    // })
}
